import {tashkentzoom} from '@shared/helpers/constants';

export const defaultMapState = {
    center: tashkentzoom,
    zoom: 12,
    behaviors: ['default', 'scrollZoom'],
    controls: ['fullscreenControl'],
};
